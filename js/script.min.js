const toggle = document.querySelector('#nav-toggle');const mobilePanel = document.querySelector('[data-nav-panel]');const siteHeader = document.querySelector('#site-header');const pageLoader = document.querySelector('#page-loader');const themeToggle = document.querySelector('#theme-toggle');const applyTheme = (theme) => {const isLight = theme === 'light';document.body.classList.toggle('light-mode', isLight);if (themeToggle) {themeToggle.textContent = isLight ? 'Dark Mode' : 'Light Mode';themeToggle.setAttribute('aria-label', isLight ? 'Switch to dark mode' : 'Switch to light mode');}};const savedTheme = localStorage.getItem('loneWolfTheme') || 'dark';applyTheme(savedTheme);if (themeToggle) {themeToggle.addEventListener('click', () => {const nextTheme = document.body.classList.contains('light-mode') ? 'dark' : 'light';localStorage.setItem('loneWolfTheme', nextTheme);applyTheme(nextTheme);});}if (pageLoader) {const hideLoader = () => {pageLoader.classList.add('is-hidden');window.setTimeout(() => {pageLoader.remove();}, 500);};if (document.readyState === 'complete') hideLoader();else window.addEventListener('load', hideLoader, { once: true });}if (window.AOS) {window.AOS.init({duration: 800,once: true,offset: 80,easing: 'ease-out-cubic'});}if (toggle && mobilePanel) {toggle.addEventListener('click', () => {const isExpanded = toggle.getAttribute('aria-expanded') === 'true';const nextState = !isExpanded;toggle.setAttribute('aria-expanded', String(nextState));toggle.classList.toggle('active', nextState);mobilePanel.classList.toggle('max-h-0', !nextState);mobilePanel.classList.toggle('opacity-0', !nextState);mobilePanel.classList.toggle('py-0', !nextState);mobilePanel.classList.toggle('max-h-80', nextState);mobilePanel.classList.toggle('opacity-100', nextState);mobilePanel.classList.toggle('py-4', nextState);});mobilePanel.querySelectorAll('a').forEach((link) => {link.addEventListener('click', () => {toggle.setAttribute('aria-expanded', 'false');toggle.classList.remove('active');mobilePanel.classList.add('max-h-0', 'opacity-0', 'py-0');mobilePanel.classList.remove('max-h-80', 'opacity-100', 'py-4');});});}if (siteHeader) {const updateHeaderState = () => {siteHeader.classList.toggle('scrolled', window.scrollY > 8);};updateHeaderState();window.addEventListener('scroll', updateHeaderState, { passive: true });}document.querySelectorAll('a[href^="#"]').forEach((anchor) => {anchor.addEventListener('click', (event) => {const targetId = anchor.getAttribute('href');if (!targetId || targetId === '#') return;const target = document.querySelector(targetId);if (!target) return;event.preventDefault();target.scrollIntoView({ behavior: 'smooth', block: 'start' });});});const revealItems = document.querySelectorAll('[data-reveal]');if (revealItems.length) {const observer = new IntersectionObserver((entries) => {entries.forEach((entry) => {if (entry.isIntersecting) {entry.target.classList.add('show');observer.unobserve(entry.target);}});},{ threshold: 0.18 });revealItems.forEach((item) => observer.observe(item));}const testimonialTrack = document.querySelector('[data-testimonial-track]');const testimonialDots = document.querySelectorAll('[data-testimonial-dot]');if (testimonialTrack && testimonialDots.length) {const totalSlides = testimonialDots.length;let currentSlide = 0;let autoSlideTimer;const goToSlide = (index) => {currentSlide = index;testimonialTrack.style.transform = `translateX(-${currentSlide * 100}%)`;testimonialDots.forEach((dot, dotIndex) => {dot.classList.toggle('active', dotIndex === currentSlide);});};const startAutoSlide = () => {autoSlideTimer = window.setInterval(() => {const next = (currentSlide + 1) % totalSlides;goToSlide(next);}, 4200);};const stopAutoSlide = () => {if (autoSlideTimer) window.clearInterval(autoSlideTimer);};testimonialDots.forEach((dot, index) => {dot.addEventListener('click', () => {goToSlide(index);stopAutoSlide();startAutoSlide();});});testimonialTrack.addEventListener('mouseenter', stopAutoSlide);testimonialTrack.addEventListener('mouseleave', startAutoSlide);goToSlide(0);startAutoSlide();}const filterContainer = document.querySelector('[data-menu-filters]');const menuCards = document.querySelectorAll('[data-menu-grid] [data-category]');if (filterContainer && menuCards.length) {const filterButtons = filterContainer.querySelectorAll('[data-filter]');const applyFilter = (category) => {menuCards.forEach((card) => {const cardCategory = card.getAttribute('data-category');const shouldShow = category === 'all' || cardCategory === category;card.classList.toggle('hidden', !shouldShow);});};filterButtons.forEach((button) => {button.addEventListener('click', () => {const selected = button.getAttribute('data-filter') || 'all';filterButtons.forEach((btn) => btn.classList.remove('active'));button.classList.add('active');applyFilter(selected);});});}const galleryButtons = document.querySelectorAll('[data-gallery-item]');const lightbox = document.querySelector('#gallery-lightbox');const lightboxImage = document.querySelector('#lightbox-image');const closeLightboxBtn = document.querySelector('[data-lightbox-close]');const prevLightboxBtn = document.querySelector('[data-lightbox-prev]');const nextLightboxBtn = document.querySelector('[data-lightbox-next]');if (galleryButtons.length && lightbox && lightboxImage) {let currentIndex = 0;const updateLightboxImage = () => {const sourceImage = galleryButtons[currentIndex].querySelector('img');if (!sourceImage) return;lightboxImage.src = sourceImage.src;lightboxImage.alt = sourceImage.alt;};const openLightbox = (index) => {currentIndex = index;updateLightboxImage();lightbox.classList.remove('hidden');lightbox.classList.add('flex');lightbox.setAttribute('aria-hidden', 'false');document.body.classList.add('overflow-hidden');};const closeLightbox = () => {lightbox.classList.add('hidden');lightbox.classList.remove('flex');lightbox.setAttribute('aria-hidden', 'true');document.body.classList.remove('overflow-hidden');};const showNext = () => {currentIndex = (currentIndex + 1) % galleryButtons.length;updateLightboxImage();};const showPrev = () => {currentIndex = (currentIndex - 1 + galleryButtons.length) % galleryButtons.length;updateLightboxImage();};galleryButtons.forEach((button, index) => {button.addEventListener('click', () => openLightbox(index));});closeLightboxBtn?.addEventListener('click', closeLightbox);nextLightboxBtn?.addEventListener('click', showNext);prevLightboxBtn?.addEventListener('click', showPrev);lightbox.addEventListener('click', (event) => {if (event.target === lightbox) closeLightbox();});document.addEventListener('keydown', (event) => {if (lightbox.classList.contains('hidden')) return;if (event.key === 'Escape') closeLightbox();if (event.key === 'ArrowRight') showNext();if (event.key === 'ArrowLeft') showPrev();});}const reservationForm = document.querySelector('[data-reservation-form]');if (reservationForm) {const statusNode = reservationForm.querySelector('[data-reservation-status]');const phonePattern = /^[+]?[\d\s\-()]{7,20}$/;const setFieldError = (name, message) => {const field = reservationForm.querySelector(`[name="${name}"]`);const errorNode = reservationForm.querySelector(`[data-error-for="${name}"]`);if (!field || !errorNode) return;errorNode.textContent = message;errorNode.style.display = message ? 'block' : 'none';field.classList.toggle('border-red-400', Boolean(message));};reservationForm.addEventListener('submit', (event) => {event.preventDefault();const formData = new FormData(reservationForm);const name = String(formData.get('name') || '').trim();const phone = String(formData.get('phone') || '').trim();const date = String(formData.get('date') || '').trim();const time = String(formData.get('time') || '').trim();const guestsRaw = String(formData.get('guests') || '').trim();const message = String(formData.get('message') || '').trim();const guests = Number(guestsRaw);const errors = {};if (!name) errors.name = 'Please enter your name.';if (!phone) errors.phone = 'Please enter your phone number.';else if (!phonePattern.test(phone)) errors.phone = 'Please enter a valid phone number.';if (!date) errors.date = 'Please select a reservation date.';else if (new Date(date) < new Date(new Date().toDateString())) errors.date = 'Date cannot be in the past.';if (!time) errors.time = 'Please select a reservation time.';if (!guestsRaw) errors.guests = 'Please enter the number of guests.';else if (!Number.isInteger(guests) || guests < 1 || guests > 12) errors.guests = 'Guests must be between 1 and 12.';if (message.length > 300) errors.message = 'Message must be 300 characters or fewer.';['name', 'phone', 'date', 'time', 'guests', 'message'].forEach((fieldName) => {setFieldError(fieldName, errors[fieldName] || '');});if (Object.keys(errors).length) {if (statusNode) statusNode.classList.add('hidden');return;}reservationForm.reset();if (statusNode) {statusNode.textContent = 'Reservation request received. We will contact you shortly to confirm.';statusNode.classList.remove('hidden');}});}const contactForm = document.querySelector('[data-contact-form]');if (contactForm) {const statusNode = contactForm.querySelector('[data-contact-status]');const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;const setFieldError = (name, message) => {const field = contactForm.querySelector(`[name="${name}"]`);const errorNode = contactForm.querySelector(`[data-error-for="${name}"]`);if (!field || !errorNode) return;errorNode.textContent = message;errorNode.style.display = message ? 'block' : 'none';field.classList.toggle('border-red-400', Boolean(message));};contactForm.addEventListener('submit', (event) => {event.preventDefault();const formData = new FormData(contactForm);const name = String(formData.get('name') || '').trim();const email = String(formData.get('email') || '').trim();const message = String(formData.get('message') || '').trim();const errors = {};if (!name) errors.name = 'Please enter your name.';if (!email) errors.email = 'Please enter your email.';else if (!emailPattern.test(email)) errors.email = 'Please enter a valid email address.';if (!message) errors.message = 'Please enter your message.';else if (message.length < 10) errors.message = 'Message should be at least 10 characters.';['name', 'email', 'message'].forEach((fieldName) => {setFieldError(fieldName, errors[fieldName] || '');});if (Object.keys(errors).length) {if (statusNode) statusNode.classList.add('hidden');return;}contactForm.reset();if (statusNode) {statusNode.textContent = 'Message sent successfully. We will get back to you soon.';statusNode.classList.remove('hidden');}});}document.querySelectorAll('form').forEach((form) => {if (form.matches('[data-reservation-form]')) return;if (form.matches('[data-contact-form]')) return;form.addEventListener('submit', (event) => {event.preventDefault();alert('Thank you. We will contact you shortly.');form.reset();});});
